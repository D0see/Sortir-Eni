<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-lg">
    <div class="container-fluid">
        <a href="{{ path('app_main') }}" title="Revenir à l'accueil">
            <img style="width: 60px;height: auto;" src="{{ asset('img/logoblanc.png') }}" alt="Logo">
        </a>
        <a class="navbar-brand" href="#" style="font-size: 200%">Sortie</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_main')}}" title="main">Menu</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sortie_list') }}">Les Sorties</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sortie_create') }}">Créé une Sortie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('participant_register') }}">Inscription</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>