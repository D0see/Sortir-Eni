{% extends 'base.html.twig' %}

{% block title %}
    Modifier mon profil
{% endblock %}

{% block body %}
    <h1 class="mb-4">Modifier mon profil</h1>

    {{ form_start(editForm) }}
    {{ form_row(editForm.pseudo) }}
    {{ form_row(editForm.prenom) }}
    {{ form_row(editForm.nom) }}
    {{ form_row(editForm.telephone) }}
    {{ form_row(editForm.mail) }}

    {# Si vous avez un champ "site" #}
    {# {{ form_row(editForm.site) }} #}

    <button type="submit" class="btn btn-primary">Enregistrer</button>
    <a href="{{ path('participant_profile') }}" class="btn btn-secondary">Annuler</a>
    {{ form_end(editForm) }}
{% endblock %}